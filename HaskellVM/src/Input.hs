--------------------------------------------------------------------------------
-- | Takes file input, gives it to parser.
module Input where

import Language

input :: String -> Either Err String
input = undefined